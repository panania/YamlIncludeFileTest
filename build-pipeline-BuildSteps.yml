# ASP.NET 
# Build and test ASP.NET projects targeting .NET Framework
# Build Definition - Build Steps

steps:
- script: |
    echo 'In the Build Steps script'

# - task: NuGetToolInstaller@0
  # displayName: 'Get Nuget Package Installer'
  # inputs:
    # versionSpec: '4.6.0'

# - task: NuGetCommand@2
  # displayName: 'Get Nuget Packages'
  # inputs:
    # restoreSolution: '$(solution)'
    
# - task: VSBuild@1
  # displayName: 'Build using VS Build'
  # inputs:
    # solution: '$(solution)'
    # vsVersion: 'latest'
    # msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:DesktopBuildPackageLocation="$(build.artifactStagingDirectory)\WebApp.zip" /p:DeployIisAppPath="Default Web Site"'
    # platform: '$(buildPlatform)'
    # configuration: '$(buildConfiguration)'